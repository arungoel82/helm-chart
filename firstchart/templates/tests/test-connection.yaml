apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx_firstchart.fullname" . }}-test-connection"
  labels:
    {{- include "nginx_firstchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx_firstchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
