apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-helm
  namespace: chart
  labels:
    app: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: svc-nginx-helm
  name: svc-nginx-helm
  namespace: chart
spec:
  ports:
  - name: http
    port: 8808
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment02-helm
  namespace: chart
  labels:
    app: nginx02
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx02
  template:
    metadata:
      labels:
        app: nginx02
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: svc-nginx02-helm
  name: svc-nginx02-helm
  namespace: kong
spec:
  ports:
  - name: http
    port: 8800
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx02
  type: ClusterIP
